#!/bin/bash
#
# (l)ast (c)ommit

set -e

# TODO: use plumbing commands?
REV="$(git --no-pager log --no-merges --author='Jim' -1 --format='%h')"

FILES="$(git diff-tree --no-commit-id --name-only -r ${REV} | tr '\n' ' ')"

vim ${FILES}
